# Evaluation / prep pipeline -- CPU only, runs on the laptop.
gemmi>=0.6
pyyaml>=6
requests>=2.28
DockQ>=2.1.0

# NOT installed here (GPU / Linux, run elsewhere):
#   colabfold[alphafold]   -> pipeline/run_colabfold.sh   (HPC or Linux+internet)
#   boltz                  -> methods/boltz               (HPC, pending scheduler details)
